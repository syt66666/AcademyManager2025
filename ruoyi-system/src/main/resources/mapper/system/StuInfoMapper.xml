<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.StuInfoMapper">
    <select id="selectByStudentId" resultType="com.ruoyi.system.domain.StuInfo">
        SELECT id, student_id, student_name, academy, system_major, major, student_class, divert_form, innovation_class, student_sex
        FROM stu_info
        WHERE student_id = #{studentId}
    </select>
</mapper>
